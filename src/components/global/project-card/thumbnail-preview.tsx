import { type Slide } from "@/lib/types";

type Props = {
  slide: Slide;
  theme: Theme;
};

const ThumbnailPreview = ({ slide }: Props) => {
  return <div>ThumbnailPreview</div>;
};

export default ThumbnailPreview;
